#  Frontend - UniApp 前端页面

负责人：吴美昕

---

# 一、预览项目
## 方法一：直接看页面效果
GitHub Pages 在线预览链接：
https://threepears6207.github.io/poetry-ai-app/

注意：在线预览主要用于查看页面效果。如果要连接真实后端接口，后端需要部署到公网地址。

---

## 方法二：本地运行项目
本项目使用 HBuilderX 打开。

**打开步骤**
1. 下载 HBuilderX
2. 文件 → 打开目录
3. 选择本项目文件夹

**运行步骤**
1. 点击运行
2. 运行到浏览器
3. Edge 或 Chrome

注意：不要直接双击 `index.html` 打开，否则页面可能无法正常跳转。

---

# 二、前端和后端联调
前端接口统一写在：`utils/api.js`

当前本地接口地址：
```js
const BASE_URL = 'http://127.0.0.1:8000'
```

**联调顺序**
1. VSCode 启动后端 FastAPI
2. 浏览器访问 `http://127.0.0.1:8000/ping`
3. HBuilderX 运行前端到 Edge 或 Chrome
4. 在前端页面测试功能

如果 `/ping` 返回 `pong`，说明后端启动成功。

---

# 三、前端接口对接情况

| 前端功能 | 后端接口 | 对接状态 | 说明 |
| --- | --- | --- | --- |
| 后端连接检测 | GET /ping | 已接入 | 检查后端是否启动 |
| 搜索古诗 | GET /poems/search | 已接入 | 支持诗名、作者、内容、标签搜索 |
| 古诗详情 | GET /poems/{poem_id} | 已接入 | 根据诗词ID获取详情 |
| 添加学习记录 | POST /record | 已接入 | 记录用户学过的诗 |
| 家长端学习统计 | GET /record/summary | 已接入 | 展示已学数量、记录 |
| 推荐古诗 | GET /recommend | 已接入 | 根据学习记录推荐 |
| AI 诗人对话 | POST /chat | 前端已接入，依赖后端配置 | 需要配置 vivo API |
| 拍照识诗 | POST /ocr | 已接入临时版 | 文字匹配，非真实图片识别 |
| AI 配图生成 | POST /generate/image | 前端已预留 | 需要 vivo API，生成慢 |
| 语音朗读 | POST /tts | 前端已预留，后端未完成 | 后端返回“接口待实现” |

---

## 当前可以稳定演示的功能
1. 首页进入项目
2. 古诗学习页搜索古诗
3. 查看古诗详情
4. 自动记录学习记录
5. 推荐页展示推荐古诗
6. 家长端查看学习统计
7. 拍照识诗临时文字匹配演示



## 说明
1. 不要直接修改 `docs` 里的代码，源码在 `pages`、`utils`、`static`。
2. 修改源码后需要重新打包 H5 并覆盖 `docs` 才能更新在线预览。
3. `unpackage` 是临时目录，不需要上传。
4. 接口失败优先检查：后端是否启动、地址是否正确、AI 配置是否正常。
5. 本地调试地址：`http://127.0.0.1:8000`。
6. 打包安卓 APK 时，需要改成电脑局域网 IP。
7. 页面样式异常请在 HBuilderX 重新运行，不要直接打开 index.html。